# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI agent worktrees
.claude/worktrees/

# Storybook build output
storybook-static

# Astro build cache
.astro

# Turborepo cache
.turbo

# Test coverage
coverage

# Playwright MCP output
.playwright-mcp

docs/superpowers/*
# Impeccable design skill (local tooling — installed per-machine, not committed):
# skill copies in every harness dir, its hook wiring, and its state/cache dirs
**/skills/impeccable/
.cursor/hooks.json
.github/hooks/impeccable.json
.impeccable/

# Claude Code personal-local settings
.claude/settings.local.json
