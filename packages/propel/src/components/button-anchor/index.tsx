// Ready-made 1:1 re-export of the ui primitive. Drop to `@plane/propel/ui/button-anchor` only for
// the lower-level part. (`ButtonAnchor` is a single styled `<button>`, so nothing extra to compose.)
export * from "../../ui/button-anchor";
